{
  "name": "projecto_p5",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "dev": "browser-sync start --open external --port 8080 -w --server public/ --files public/*",
    "paddle": "browser-sync start --open external --port 8080 -w --server paddle/ --files paddle/*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.27.4"
  }
}
